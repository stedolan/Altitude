#include "hashtable.h"

/*
This file must give a full definition of struct ht_int and struct ht_str

I recommend that both hashtable types use the same structure internally
i.e. reuse the same hashtable type.
*/
