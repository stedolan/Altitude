This directory holds or will hold the code for the Altitude virtual
machine, written in C. This is purely the internal VM portion of
Altitude, and contains no GUI code (see the ui/ directory for that).
